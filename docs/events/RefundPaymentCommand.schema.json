{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://pos.local/events/RefundPaymentCommand.schema.json",
  "title": "RefundPaymentCommand",
  "type": "object",
  "required": ["event_id","event_name","event_version","occurred_at","correlation_id","payload"],
  "properties": {
    "event_id": { "type": "string", "format": "uuid" },
    "event_name": { "const": "RefundPaymentCommand" },
    "event_version": { "type": "integer", "minimum": 1 },
    "occurred_at": { "type": "string", "format": "date-time" },
    "correlation_id": { "type": "string" },
    "saga_id": { "type": "string", "format": "uuid" },
    "payload": {
      "type": "object",
      "required": ["payment_id","mp_payment_id","reason"]
    }
  }
}
